<%= simple_form_for(@party) do |f| %>
  <%= f.input :name %>
  <%= f.input :user_ids, as: :check_boxes, collection: current_user.friends.map { |e| [e.full_name, e.id] } %>
  <%= f.submit %>
<% end %>
<%= link_to "Cancel", dashboard_path %>
